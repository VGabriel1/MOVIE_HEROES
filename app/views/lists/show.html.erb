<p><strong>New list</strong></p>
  <div class="list-group">
    <%= @list.name %>
    <%= @list.genre %>
  </div>

  <%= link_to "Edit",
    edit_list_path(@list)%>

  <%= link_to "Delete",
    list_path(@list),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
  %>
